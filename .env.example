# Copy to .env and fill in. Keys can also come from the shell environment.
ARCADE_API_KEY=arc_...
ANTHROPIC_API_KEY=sk-ant-...
# AGENT_MODEL=claude-opus-4-8
ARCADE_SA_USER=agent-service@example.com   # identity that sends consent emails
ARCADE_TOOLKITS=gmail,googlecalendar       # toolkits each child agent gets
ARCADE_SA_TOOLKITS=gmail                   # toolkits the service account gets (email only)
# MEMORY_CAP_TOKENS=4000                  # per-user memory budget (tokens)
# MEMORY_MODEL=claude-haiku-4-5-20251001  # small model for memory compaction
